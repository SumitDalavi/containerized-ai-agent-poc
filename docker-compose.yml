version: '3.8'

services:
  agent-service:
    build: .
    container_name: fastapi-ai-agent
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=development
    volumes:
      - ./app:/code/app
    restart: always
